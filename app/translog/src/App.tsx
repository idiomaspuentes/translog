// Entry point component — the actual UI is rendered by the TanStack Router
// tree in main.tsx. This file is kept for compatibility with the Vite
// project scaffold but is not used at runtime.
export default function App() {
  return null
}
